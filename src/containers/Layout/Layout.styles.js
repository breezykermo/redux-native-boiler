import { colors } from '../../common/theme'

export default {
	container: {
		backgroundColor: colors.background,
		flex: 1,
		padding: 0
	},
	body: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center'
	}
}
