import { Dimensions } from 'react-native'

const _W = Dimensions.get('window')
export const { height: DEVICE_HEIGHT, width: DEVICE_WIDTH } = _W

export const STATUS_BAR_HEIGHT = 20
export const NAVIGATION_BAR_HEIGHT = 44
export const STATUS_NAV_HEIGHT = 64
